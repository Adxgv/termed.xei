<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Lookup</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;600&display=swap');
        
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: black;
            color: white;
            font-family: 'Quicksand', sans-serif;
            text-align: center;
            overflow: hidden;
        }
        .box {
            background: #220000;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(255, 0, 0, 0.5);
            transform: scale(0.7);
            animation: zoomIn 0.5s ease-out forwards;
        }
        @keyframes zoomIn {
            from { transform: scale(0.7); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
        .status {
            font-size: 24px;
            font-weight: 600;
            color: red;
            margin-bottom: 10px;
        }
        .username-box {
            background: mediumseagreen;
            color: white;
            font-size: 18px;
            font-weight: 600;
            padding: 10px;
            border-radius: 5px;
            display: inline-block;
            cursor: pointer;
            transition: transform 0.2s ease-out;
        }
        .username-box:hover {
            transform: scale(1.1);
        }
        .buttons {
            margin-top: 15px;
        }
        .btn {
            background-color: mediumseagreen;
            color: white;
            border: none;
            padding: 10px 15px;
            font-size: 14px;
            font-weight: 600;
            border-radius: 5px;
            margin: 5px;
            cursor: pointer;
            transition: transform 0.2s ease-out, box-shadow 0.2s ease-out;
            display: inline-block;
        }
        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(60, 179, 113, 0.3);
        }
        .btn:active {
            transform: scale(0.95);
            box-shadow: 0 3px 10px rgba(60, 179, 113, 0.5);
        }
    </style>
</head>
<body>
    <div class="box">
        <div class="status">Termed = <span style="color: red;">True</span></div>
        <div class="username-box" id="username" onclick="copyUsername()">xei111</div>
        <p><strong>xei.111</strong> has been termed.</p>
        <div class="buttons">
            <button class="btn" onclick="openProfile()">Open New Discord Profile</button>
        </div>
    </div>

    <script>
        async function copyUsername() {
            try {
                const username = document.getElementById("username").textContent;
                await navigator.clipboard.writeText(username);
                alert("Copied: " + username);
            } catch (err) {
                console.error("Failed to copy: ", err);
                alert("Failed to copy username. Please try again.");
            }
        }

        function openProfile() {
            window.location.href = "https://discord.com/users/1354941447832604793";
        }
    </script>
</body>
</html>
